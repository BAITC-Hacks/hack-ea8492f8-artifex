# Optional. The sample and deterministic analysis work without an API key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Optional: a trusted endpoint implementing Responses API, strict JSON schemas and tool calls.
# A generic OpenAI-compatible chat endpoint is not sufficient.
OPENAI_BASE_URL=
# Optional: Python interpreter with pypdf/openpyxl installed for PDF/Excel input.
PYTHON_BIN=python3
PORT=4173
